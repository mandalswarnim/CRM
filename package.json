{
  "name": "meridian-crm",
  "version": "1.0.0",
  "private": true,
  "description": "Meridian — a Salesforce-compatible, metadata-driven CRM platform, delivered with a London private members' club sample org",
  "type": "module",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=server & npm run dev --workspace=client & wait",
    "dev:server": "npm run dev --workspace=server",
    "dev:client": "npm run dev --workspace=client",
    "build": "npm run build --workspace=server && npm run build --workspace=client",
    "test": "npm run test --workspace=server",
    "seed": "npm run seed --workspace=server",
    "start": "npm run start --workspace=server",
    "dataloader": "node scripts/dataloader.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
